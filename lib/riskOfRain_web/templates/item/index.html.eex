<center>
  <form style="font-size : 30px; width: 500px;
      height: 100%; background:#48E0A7;
      border-radius: 25px; border: 2px solid black;
      padding:2px">
      <strong>Items</strong>
  </form>
</center>

<h2>
  <center>
    <form  style="font-size : 20px; width: 500px;
          height: 100%; background:#48E0A7;
          border-radius: 25px; border: 2px solid black;
          padding:6px">

      <%= Phoenix.HTML.Link.button("Home", to: "/", method: "get", class: "btn") %>
      <%= Phoenix.HTML.Link.button("Characters", to: "/characters", method: "get", class: "btn") %>
      <%= Phoenix.HTML.Link.button("Enemies", to: "/enemies", method: "get", class: "btn") %>
      <%= Phoenix.HTML.Link.button("Items", to: "/items", method: "get", class: "btn") %>
    </form>
  </center>
</h2>

<table class="table"
      style="font-size : 15px; width: 100%;
      height: 100%; background:#48E0A7;
      border-radius: 25px; border: 2px solid black;
      border-collapse: separate !important;
      padding:6px; table-layout: fixed; word-wrap: break-word;">
  <thead>
    <tr>
      <th>Name</th>
      <th>Desc</th>
      <th>Rarity</th>

      <th>
        <center>
          <span style="font-size: 15px; color: black;">
            <%= link "New Item", to: item_path(@conn, :new) %>
          </span>
        </center>
      </th>
    </tr>
  </thead>
  <tbody>
<%= for item <- @items do %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.desc %></td>
      <td><%= item.rarity %></td>

      <td class="text-right">
        <span><%= link "Show", to: item_path(@conn, :show, item), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: item_path(@conn, :edit, item), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: item_path(@conn, :delete, item), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>
